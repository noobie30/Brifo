VITE_DOWNLOAD_URL=#
# Backend API base URL (must include /api). Local dev:
# VITE_API_URL=http://localhost:3001/api
# Production (this monorepo’s Vercel deploy — use your real domain if different):
# VITE_API_URL=https://api.brifo.in
VITE_API_URL=
